{
  "name": "goensemble",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "bun --cwd apps/mobile start",
    "android": "bun --cwd apps/mobile android",
    "ios": "bun --cwd apps/mobile ios",
    "api": "bun run --cwd apps/api dev",
    "db:generate": "bun run --cwd packages/database generate",
    "db:migrate": "bun run --cwd packages/database migrate:dev"
  }
}